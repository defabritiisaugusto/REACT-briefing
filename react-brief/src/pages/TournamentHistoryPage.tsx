// Pagina sottile: importa il componente TournamentHistory
// e si limita a renderizzarlo.

import { TournamentHistory } from "@/features/tournament/TournamentHistory";

const TournamentHistoryPage = () => {
  return <TournamentHistory />;
};

export default TournamentHistoryPage;
